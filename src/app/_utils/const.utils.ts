import {environment} from "../../environments/environment";

export const apiRoutes = {
  base: environment.baseUrl,
  biking: 'biking/',
  bodyShape: 'body-shape/',
  bikingStats: 'biking-stats'
}

export const wsRoutes = {
  organisation: 'organisation/',
  room: 'room'
}
