import { FormatStatsPipe } from './format-stats.pipe';

describe('FormatStatsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStatsPipe();
    expect(pipe).toBeTruthy();
  });
});
